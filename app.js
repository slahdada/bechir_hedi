const translations = {
  fr: {
    eyebrow: "Tableau intelligent",
    title: "Suivi moderne des produits et des frais",
    subtitle: "Ajoutez des lignes, collez des photos, suivez les frais et obtenez les totaux en temps reel.",
    languageLabel: "Langue",
    addRow: "Ajouter une ligne",
    product: "Produit",
    referenceCode: "Code de reference",
    photo: "Photo",
    caption: "Legende",
    pickupFee: "Frais de recuperation",
    deliveryFee: "Frais de livraison",
    productPrice: "Prix du produit",
    netTotal: "Total net",
    notes: "Notes / reclamations",
    actions: "Actions",
    totalProducts: "Total produits",
    totalFees: "Total frais",
    remainingNet: "Reste net",
    mobileHint: "Version optimisee pour mobile et bureau",
    exportTitle: "Telechargement du contenu",
    exportSubtitle: "Exportez la page en plusieurs formats selon votre besoin.",
    exportPdf: "Exporter PDF",
    exportImage: "Exporter image",
    exportPage: "Exporter page",
    exportJson: "Exporter JSON",
    exportCsv: "Exporter CSV",
    exportReady: "Export pret.",
    autosaveReady: "Sauvegarde automatique active.",
    autosaveSaved: "Modifications enregistrees automatiquement.",
    autosaveRestored: "Travail restaure apres reouverture.",
    autosaveNever: "Aucune sauvegarde precedente.",
    lastSavedLabel: "Derniere sauvegarde : {time}",
    clearSavedData: "Effacer les donnees sauvegardees",
    clearSavedDone: "Donnees sauvegardees effacees.",
    exportImagePreparing: "Preparation de l'image...",
    exportPdfHint: "La fenetre d'impression est ouverte pour enregistrer en PDF.",
    exportDone: "Telechargement lance.",
    exportError: "Export impossible pour le moment.",
    uploadPhoto: "Ajouter ou coller une photo",
    takePhoto: "Prendre une photo",
    captionPlaceholder: "Legende",
    captionDetailPlaceholder: "Numero ☎",
    clientNamePlaceholder: "Nom client",
    clientAddressPlaceholder: "Mini adresse",
    pastePhoto: "Coller",
    delete: "Supprimer",
    productPlaceholder: "Nom du produit",
    referencePlaceholder: "Reference",
    notesPlaceholder: "Ajouter une note ou une reclamation"
  },
  ar: {
    eyebrow: "جدول ذكي",
    title: "متابعة حديثة للمنتجات والمصاريف",
    subtitle: "أضف صفوفًا والصق الصور وتابع المصاريف واحصل على المجاميع بشكل فوري.",
    languageLabel: "اللغة",
    addRow: "إضافة سطر",
    product: "المنتج",
    referenceCode: "الرمز المرجعي",
    photo: "الصورة",
    caption: "التسمية التوضيحية",
    pickupFee: "مصاريف الاستلام",
    deliveryFee: "مصاريف التوصيل",
    productPrice: "سعر المنتج",
    netTotal: "الصافي",
    notes: "ملاحظات / شكاوى",
    actions: "إجراءات",
    totalProducts: "إجمالي المنتجات",
    totalFees: "إجمالي المصاريف",
    remainingNet: "الصافي المتبقي",
    mobileHint: "نسخة محسنة للهاتف والكمبيوتر",
    exportTitle: "تنزيل المحتوى",
    exportSubtitle: "صدّر الصفحة بعدة صيغ حسب الحاجة.",
    exportPdf: "تصدير PDF",
    exportImage: "تصدير صورة",
    exportPage: "تصدير الصفحة",
    exportJson: "تصدير JSON",
    exportCsv: "تصدير CSV",
    exportReady: "التصدير جاهز.",
    autosaveReady: "الحفظ التلقائي مفعل.",
    autosaveSaved: "تم حفظ التعديلات تلقائيا.",
    autosaveRestored: "تمت استعادة العمل بعد إعادة الفتح.",
    autosaveNever: "لا توجد عملية حفظ سابقة.",
    lastSavedLabel: "آخر حفظ: {time}",
    clearSavedData: "مسح البيانات المحفوظة",
    clearSavedDone: "تم مسح البيانات المحفوظة.",
    exportImagePreparing: "جار تحضير الصورة...",
    exportPdfHint: "تم فتح نافذة الطباعة للحفظ كملف PDF.",
    exportDone: "بدأ التنزيل.",
    exportError: "التصدير غير متاح حاليا.",
    uploadPhoto: "أضف صورة أو الصقها",
    takePhoto: "التقاط صورة",
    captionPlaceholder: "التسمية",
    captionDetailPlaceholder: "رقم ☎",
    clientNamePlaceholder: "اسم العميل",
    clientAddressPlaceholder: "عنوان مختصر",
    pastePhoto: "لصق",
    delete: "حذف",
    productPlaceholder: "اسم المنتج",
    referencePlaceholder: "المرجع",
    notesPlaceholder: "أضف ملاحظة أو شكوى"
  },
  en: {
    eyebrow: "Smart table",
    title: "Modern product and expense tracking",
    subtitle: "Add rows, paste photos, track fees, and get live totals instantly.",
    languageLabel: "Language",
    addRow: "Add row",
    product: "Product",
    referenceCode: "Reference code",
    photo: "Photo",
    caption: "Caption",
    pickupFee: "Pickup fee",
    deliveryFee: "Delivery fee",
    productPrice: "Product price",
    netTotal: "Net total",
    notes: "Notes / complaints",
    actions: "Actions",
    totalProducts: "Products total",
    totalFees: "Fees total",
    remainingNet: "Net remaining",
    mobileHint: "Optimized for mobile and desktop",
    exportTitle: "Download content",
    exportSubtitle: "Export the page in several formats depending on your need.",
    exportPdf: "Export PDF",
    exportImage: "Export image",
    exportPage: "Export page",
    exportJson: "Export JSON",
    exportCsv: "Export CSV",
    exportReady: "Export ready.",
    autosaveReady: "Auto-save is active.",
    autosaveSaved: "Changes saved automatically.",
    autosaveRestored: "Work restored after reopening.",
    autosaveNever: "No previous save.",
    lastSavedLabel: "Last saved: {time}",
    clearSavedData: "Clear saved data",
    clearSavedDone: "Saved data cleared.",
    exportImagePreparing: "Preparing image...",
    exportPdfHint: "The print window is open to save as PDF.",
    exportDone: "Download started.",
    exportError: "Export is not available right now.",
    uploadPhoto: "Add or paste a photo",
    takePhoto: "Take photo",
    captionPlaceholder: "Caption",
    captionDetailPlaceholder: "Number ☎",
    clientNamePlaceholder: "Client name",
    clientAddressPlaceholder: "Mini address",
    pastePhoto: "Paste",
    delete: "Delete",
    productPlaceholder: "Product name",
    referencePlaceholder: "Reference",
    notesPlaceholder: "Add a note or complaint"
  }
};

const state = {
  language: "fr",
  formatter: new Intl.NumberFormat("fr-FR", { minimumFractionDigits: 2, maximumFractionDigits: 2 })
};

const tableBody = document.getElementById("tableBody");
const rowTemplate = document.getElementById("rowTemplate");
const addRowBtn = document.getElementById("addRowBtn");
const floatingAddBtn = document.getElementById("floatingAddBtn");
const languageSelect = document.getElementById("languageSelect");
const exportStatus = document.getElementById("exportStatus");
const exportButtons = document.querySelectorAll(".export-btn");
const clearSavedDataBtn = document.getElementById("clearSavedDataBtn");
const autosaveMeta = document.getElementById("autosaveMeta");
const STORAGE_KEY = "product-tracker-autosave-v1";
let autosaveTimer = null;
let lastSavedAt = null;

function getText(key) {
  return translations[state.language][key] || "";
}

function updateAutosaveMeta() {
  if (!lastSavedAt) {
    autosaveMeta.textContent = getText("autosaveNever");
    return;
  }
  const locale = state.language === "ar" ? "ar-TN" : state.language === "en" ? "en-US" : "fr-FR";
  const formatted = new Intl.DateTimeFormat(locale, {
    dateStyle: "short",
    timeStyle: "short"
  }).format(lastSavedAt);
  autosaveMeta.textContent = getText("lastSavedLabel").replace("{time}", formatted);
}

function parseNumber(value) {
  const parsed = Number.parseFloat(value);
  return Number.isFinite(parsed) ? parsed : 0;
}

function formatCurrency(value) {
  return state.formatter.format(value);
}

function slugify(value) {
  return value
    .toLowerCase()
    .replace(/[^a-z0-9\u0600-\u06ff]+/g, "-")
    .replace(/^-+|-+$/g, "") || "export";
}

function buildFileName(extension) {
  const title = getText("title") || "export";
  const date = new Date().toISOString().slice(0, 19).replace(/[:T]/g, "-");
  return `${slugify(title)}-${date}.${extension}`;
}

function setExportStatus(key) {
  exportStatus.textContent = getText(key);
}

function getRowData(row) {
  const product = row.querySelector('[data-field="product"]').value.trim();
  const reference = row.querySelector('[data-field="reference"]').value.trim();
  const caption = row.querySelector('[data-field="captionDetail"]').value.trim();
  const pickupFee = parseNumber(row.querySelector('[data-field="pickupFee"]').value);
  const deliveryFee = parseNumber(row.querySelector('[data-field="deliveryFee"]').value);
  const price = parseNumber(row.querySelector('[data-field="price"]').value);
  const total = price - (pickupFee + deliveryFee);
  const notes = row.querySelector('[data-field="notes"]').value.trim();
  const image = row.querySelector(".photo-preview").hidden ? "" : row.querySelector(".photo-preview").src;
  const contact = {
    captionDetail: row.querySelector('[data-field="captionDetail"]').value.trim(),
    name: row.querySelector('[data-field="clientName1"]').value.trim(),
    address: row.querySelector('[data-field="clientAddress1"]').value.trim()
  };
  return { product, reference, caption, pickupFee, deliveryFee, price, total, notes, image, contact };
}

function getAllData() {
  const rows = [...tableBody.rows].map(getRowData);
  const totals = {
    totalProducts: document.getElementById("totalProducts").textContent,
    totalFees: document.getElementById("totalFees").textContent,
    remainingNet: document.getElementById("remainingNet").textContent
  };
  return {
    language: state.language,
    exportedAt: new Date().toISOString(),
    rows,
    totals
  };
}

function saveToStorage(showStatus = false) {
  try {
    const payload = getAllData();
    payload.lastSavedAt = new Date().toISOString();
    localStorage.setItem(STORAGE_KEY, JSON.stringify(payload));
    lastSavedAt = new Date(payload.lastSavedAt);
    updateAutosaveMeta();
    if (showStatus) {
      setExportStatus("autosaveSaved");
    }
  } catch (error) {
    console.error(error);
  }
}

function queueAutosave(showStatus = false) {
  clearTimeout(autosaveTimer);
  autosaveTimer = setTimeout(() => saveToStorage(showStatus), 250);
}

function applyRowData(row, rowData) {
  row.querySelector('[data-field="product"]').value = rowData.product || "";
  row.querySelector('[data-field="reference"]').value = rowData.reference || "";
  row.querySelector('[data-field="captionDetail"]').value = rowData.contact?.captionDetail || rowData.caption || "";
  row.querySelector('[data-field="clientName1"]').value = rowData.contact?.name || "";
  row.querySelector('[data-field="clientAddress1"]').value = rowData.contact?.address || "";
  row.querySelector('[data-field="pickupFee"]').value = rowData.pickupFee ?? 0;
  row.querySelector('[data-field="deliveryFee"]').value = rowData.deliveryFee ?? 0;
  row.querySelector('[data-field="price"]').value = rowData.price ?? 0;
  row.querySelector('[data-field="notes"]').value = rowData.notes || "";
  if (rowData.image) {
    const preview = row.querySelector(".photo-preview");
    const text = row.querySelector(".upload-text");
    preview.src = rowData.image;
    preview.hidden = false;
    text.hidden = true;
  }
}

function restoreFromStorage() {
  try {
    const raw = localStorage.getItem(STORAGE_KEY);
    if (!raw) {
      return false;
    }
    const saved = JSON.parse(raw);
    if (!saved || !Array.isArray(saved.rows)) {
      return false;
    }
    lastSavedAt = saved.lastSavedAt ? new Date(saved.lastSavedAt) : null;
    state.language = saved.language || "fr";
    languageSelect.value = state.language;
    tableBody.innerHTML = "";
    saved.rows.forEach((rowData) => {
      const row = rowTemplate.content.firstElementChild.cloneNode(true);
      tableBody.appendChild(row);
      bindRowEvents(row);
      applyRowData(row, rowData);
    });
    if (saved.rows.length === 0) {
      addRow(false);
    }
    applyTranslations();
    updateSummary();
    setExportStatus("autosaveRestored");
    updateAutosaveMeta();
    return true;
  } catch (error) {
    console.error(error);
    return false;
  }
}

function downloadBlob(blob, fileName) {
  const url = URL.createObjectURL(blob);
  const link = document.createElement("a");
  link.href = url;
  link.download = fileName;
  link.click();
  setTimeout(() => URL.revokeObjectURL(url), 1000);
}

function downloadDataUrl(dataUrl, fileName) {
  const link = document.createElement("a");
  link.href = dataUrl;
  link.download = fileName;
  link.click();
}

function escapeHtml(value) {
  return String(value)
    .replace(/&/g, "&amp;")
    .replace(/</g, "&lt;")
    .replace(/>/g, "&gt;")
    .replace(/"/g, "&quot;");
}

function collectDocumentStyles() {
  return [...document.styleSheets]
    .map((sheet) => {
      try {
        return [...sheet.cssRules].map((rule) => rule.cssText).join("\n");
      } catch (error) {
        return "";
      }
    })
    .join("\n");
}

function createSnapshotDocument() {
  const shell = document.querySelector(".page-shell").cloneNode(true);
  const styles = collectDocumentStyles();
  shell.querySelectorAll("input, textarea, select").forEach((field) => {
    if (field.tagName === "TEXTAREA") {
      const replacement = document.createElement("div");
      replacement.className = "export-static export-static-textarea";
      replacement.textContent = field.value;
      field.replaceWith(replacement);
    } else if (field.tagName === "SELECT") {
      const replacement = document.createElement("div");
      replacement.className = "export-static";
      replacement.textContent = field.options[field.selectedIndex]?.textContent || field.value;
      field.replaceWith(replacement);
    } else {
      const replacement = document.createElement("div");
      replacement.className = "export-static";
      replacement.textContent = field.value;
      field.replaceWith(replacement);
    }
  });
  shell.querySelectorAll("button, .floating-add-btn, .export-panel, .paste-btn, .delete-btn").forEach((node) => node.remove());
  const snapshot = `<!DOCTYPE html>
<html lang="${state.language}" dir="${state.language === "ar" ? "rtl" : "ltr"}">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>${escapeHtml(getText("title"))}</title>
  <style>${styles}
  .export-static {
    min-height: 48px;
    padding: 12px 14px;
    border: 1px solid rgba(21, 47, 46, 0.14);
    border-radius: 14px;
    background: rgba(255, 255, 255, 0.94);
    color: inherit;
    white-space: pre-wrap;
    overflow-wrap: anywhere;
  }
  .export-static-textarea {
    min-height: 92px;
  }
  .toolbar,
  .mobile-intro,
  .export-status,
  .floating-add-btn {
    display: none !important;
  }
  </style>
</head>
<body>
  ${shell.outerHTML}
</body>
</html>`;
  return snapshot;
}

function createSnapshotElement() {
  const wrapper = document.createElement("div");
  wrapper.innerHTML = createSnapshotDocument();
  return wrapper.querySelector(".page-shell");
}

function exportJson() {
  const blob = new Blob([JSON.stringify(getAllData(), null, 2)], { type: "application/json;charset=utf-8" });
  downloadBlob(blob, buildFileName("json"));
}

function csvCell(value) {
  return `"${String(value ?? "").replace(/"/g, "\"\"")}"`;
}

function exportCsv() {
  const dict = translations[state.language];
  const rows = getAllData().rows;
  const header = [
    dict.product,
    dict.referenceCode,
    dict.caption,
    dict.pickupFee,
    dict.deliveryFee,
    dict.productPrice,
    dict.netTotal,
    dict.notes,
    dict.photo
  ].map(csvCell).join(",");
  const lines = rows.map((row) => [
    row.product,
    row.reference,
    row.caption,
    row.pickupFee,
    row.deliveryFee,
    row.price,
    row.total,
    row.notes,
    row.image ? "image" : ""
  ].map(csvCell).join(","));
  const csv = [header, ...lines].join("\n");
  downloadBlob(new Blob([csv], { type: "text/csv;charset=utf-8" }), buildFileName("csv"));
}

function exportHtml() {
  downloadBlob(new Blob([createSnapshotDocument()], { type: "text/html;charset=utf-8" }), buildFileName("html"));
}

function exportPdf() {
  setExportStatus("exportPdfHint");
  window.print();
}

async function exportImage() {
  setExportStatus("exportImagePreparing");
  try {
    const canvas = document.createElement("canvas");
    const rows = getAllData().rows;
    const width = 1400;
    const margin = 40;
    const headerHeight = 140;
    const summaryHeight = 130;
    const rowHeight = 220;
    const tableHeaderHeight = 54;
    const height = margin * 2 + headerHeight + tableHeaderHeight + summaryHeight + Math.max(rows.length, 1) * rowHeight;
    canvas.width = width;
    canvas.height = height;
    const context = canvas.getContext("2d");
    if (!context) {
      throw new Error("Canvas context unavailable");
    }
    const locale = state.language === "ar" ? "ar-TN" : state.language === "en" ? "en-US" : "fr-FR";
    const fmt = new Intl.NumberFormat(locale, { minimumFractionDigits: 2, maximumFractionDigits: 2 });
    const dict = translations[state.language];

    context.fillStyle = "#f7f5ef";
    context.fillRect(0, 0, width, height);

    const gradient = context.createLinearGradient(0, 0, width, height);
    gradient.addColorStop(0, "#fff3dd");
    gradient.addColorStop(0.5, "#e9f4ef");
    gradient.addColorStop(1, "#d7e7ff");
    context.fillStyle = gradient;
    context.fillRect(0, 0, width, 220);

    context.fillStyle = "#0c5f55";
    context.font = "700 20px Segoe UI";
    context.fillText(getText("eyebrow"), margin, 42);
    context.fillStyle = "#142020";
    context.font = "700 46px Segoe UI";
    context.fillText(getText("title"), margin, 96);
    context.fillStyle = "#597070";
    context.font = "24px Segoe UI";
    context.fillText(getText("subtitle"), margin, 130);

    const columns = [
      { key: "product", label: dict.product, width: 170 },
      { key: "reference", label: dict.referenceCode, width: 170 },
      { key: "photo", label: dict.photo, width: 220 },
      { key: "caption", label: dict.caption, width: 170 },
      { key: "pickupFee", label: dict.pickupFee, width: 150 },
      { key: "deliveryFee", label: dict.deliveryFee, width: 150 },
      { key: "price", label: dict.productPrice, width: 150 },
      { key: "net", label: dict.netTotal, width: 140 },
      { key: "notes", label: dict.notes, width: 180 }
    ];

    let y = margin + headerHeight;
    let x = margin;
    context.fillStyle = "#e6f3ef";
    context.fillRect(margin, y, width - margin * 2, tableHeaderHeight);
    context.strokeStyle = "rgba(11, 143, 120, 0.12)";
    context.strokeRect(margin, y, width - margin * 2, tableHeaderHeight);
    context.fillStyle = "#0c5f55";
    context.font = "700 17px Segoe UI";
    columns.forEach((col) => {
      context.fillText(col.label, x + 10, y + 33);
      x += col.width;
    });
    y += tableHeaderHeight;

    async function loadImage(src) {
      return await new Promise((resolve) => {
        if (!src) {
          resolve(null);
          return;
        }
        const img = new Image();
        img.onload = () => resolve(img);
        img.onerror = () => resolve(null);
        img.src = src;
      });
    }

    for (const row of rows) {
      x = margin;
      context.fillStyle = "rgba(255,255,255,0.92)";
      context.fillRect(margin, y, width - margin * 2, rowHeight);
      context.strokeStyle = "rgba(24, 34, 34, 0.08)";
      context.strokeRect(margin, y, width - margin * 2, rowHeight);

      const values = {
        product: row.product,
        reference: row.reference,
        caption: row.contact?.captionDetail || row.caption,
        pickupFee: fmt.format(row.pickupFee || 0),
        deliveryFee: fmt.format(row.deliveryFee || 0),
        price: fmt.format(row.price || 0),
        net: fmt.format(row.total || 0),
        notes: row.notes
      };

      for (const col of columns) {
        context.strokeStyle = "rgba(24, 34, 34, 0.06)";
        context.beginPath();
        context.moveTo(x, y);
        context.lineTo(x, y + rowHeight);
        context.stroke();

        if (col.key === "photo") {
          const img = await loadImage(row.image);
          const boxX = x + 10;
          const boxY = y + 10;
          const boxW = col.width - 20;
          const boxH = 120;
          context.fillStyle = "#eef7f4";
          context.fillRect(boxX, boxY, boxW, boxH);
          if (img) {
            context.drawImage(img, boxX, boxY, boxW, boxH);
          } else {
            context.fillStyle = "#6b7d7d";
            context.font = "18px Segoe UI";
            context.fillText(dict.photo, boxX + 12, boxY + 34);
          }
          context.fillStyle = "#677070";
          context.font = "700 16px Segoe UI";
          context.fillText(row.contact?.name || "", boxX, y + 158);
          context.font = "14px Segoe UI";
          context.fillText(row.contact?.address || "", boxX, y + 184);
        } else {
          context.fillStyle = "#1b2424";
          context.font = col.key === "net" ? "700 16px Segoe UI" : "15px Segoe UI";
          wrapCanvasText(context, values[col.key] || "", x + 10, y + 28, col.width - 20, 20, 8);
        }
        x += col.width;
      }
      y += rowHeight;
    }

    const totalsY = y + 24;
    const summaryWidth = (width - margin * 2 - 24) / 3;
    const summaryItems = [
      { label: dict.totalProducts, value: document.getElementById("totalProducts").textContent, color: "#0f9f82" },
      { label: dict.totalFees, value: document.getElementById("totalFees").textContent, color: "#efae43" },
      { label: dict.remainingNet, value: document.getElementById("remainingNet").textContent, color: "#3a78f2" }
    ];
    summaryItems.forEach((item, index) => {
      const sx = margin + index * (summaryWidth + 12);
      context.fillStyle = item.color;
      context.fillRect(sx, totalsY, summaryWidth, 96);
      context.fillStyle = "#ffffff";
      context.font = "16px Segoe UI";
      context.fillText(item.label, sx + 16, totalsY + 32);
      context.font = "700 30px Segoe UI";
      context.fillText(item.value, sx + 16, totalsY + 72);
    });

    const pngBlob = await new Promise((resolve, reject) => {
      canvas.toBlob((blob) => {
        if (blob) {
          resolve(blob);
        } else {
          reject(new Error("PNG blob generation failed"));
        }
      }, "image/png");
    });
    downloadBlob(pngBlob, buildFileName("png"));
  } catch (error) {
    try {
      const fallbackCanvas = document.createElement("canvas");
      fallbackCanvas.width = 1200;
      fallbackCanvas.height = 800;
      const fallbackContext = fallbackCanvas.getContext("2d");
      if (!fallbackContext) {
        throw error;
      }
      fallbackContext.fillStyle = "#ffffff";
      fallbackContext.fillRect(0, 0, fallbackCanvas.width, fallbackCanvas.height);
      fallbackContext.fillStyle = "#142020";
      fallbackContext.font = "700 34px Segoe UI";
      fallbackContext.fillText(getText("title"), 40, 70);
      fallbackContext.fillStyle = "#597070";
      fallbackContext.font = "20px Segoe UI";
      fallbackContext.fillText(getText("subtitle"), 40, 108);
      fallbackContext.font = "18px Segoe UI";
      let y = 160;
      for (const row of getAllData().rows) {
        fallbackContext.fillStyle = "#eef7f4";
        fallbackContext.fillRect(40, y, 1120, 86);
        fallbackContext.fillStyle = "#142020";
        fallbackContext.font = "700 18px Segoe UI";
        fallbackContext.fillText(row.product || "-", 56, y + 30);
        fallbackContext.font = "16px Segoe UI";
        fallbackContext.fillText(`${getText("referenceCode")}: ${row.reference || "-"}`, 56, y + 56);
        fallbackContext.fillText(`${getText("productPrice")}: ${formatCurrency(row.price || 0)}`, 420, y + 30);
        fallbackContext.fillText(`${getText("netTotal")}: ${formatCurrency(row.total || 0)}`, 420, y + 56);
        fallbackContext.fillText(`${getText("caption")}: ${row.caption || "-"}`, 760, y + 30);
        fallbackContext.fillText(`${getText("notes")}: ${row.notes || "-"}`, 760, y + 56);
        y += 100;
        if (y > 700) break;
      }
      downloadDataUrl(fallbackCanvas.toDataURL("image/png"), buildFileName("png"));
    } catch (fallbackError) {
      console.error(error);
      console.error(fallbackError);
      throw fallbackError;
    }
  }
}

function wrapCanvasText(context, text, x, y, maxWidth, lineHeight, maxLines) {
  const words = String(text || "").split(/\s+/);
  let line = "";
  let lines = 0;
  for (const word of words) {
    const testLine = line ? `${line} ${word}` : word;
    if (context.measureText(testLine).width > maxWidth && line) {
      context.fillText(line, x, y + lines * lineHeight);
      line = word;
      lines += 1;
      if (lines >= maxLines) return;
    } else {
      line = testLine;
    }
  }
  if (line && lines < maxLines) {
    context.fillText(line, x, y + lines * lineHeight);
  }
}

async function handleExport(type) {
  try {
    updateSummary();
    if (type === "pdf") exportPdf();
    if (type === "png") await exportImage();
    if (type === "html") exportHtml();
    if (type === "json") exportJson();
    if (type === "csv") exportCsv();
    if (type !== "pdf") setExportStatus("exportDone");
  } catch (error) {
    console.error(error);
    setExportStatus("exportError");
  }
}

function calculateRow(row) {
  const pickup = parseNumber(row.querySelector('[data-field="pickupFee"]').value);
  const delivery = parseNumber(row.querySelector('[data-field="deliveryFee"]').value);
  const price = parseNumber(row.querySelector('[data-field="price"]').value);
  const total = price - (pickup + delivery);
  row.querySelector(".row-total").textContent = formatCurrency(total);
  return { pickup, delivery, price, total };
}

function updateSummary() {
  let totalProducts = 0;
  let totalFees = 0;
  let remainingNet = 0;

  [...tableBody.rows].forEach((row) => {
    const { pickup, delivery, price, total } = calculateRow(row);
    totalProducts += price;
    totalFees += pickup + delivery;
    remainingNet += total;
  });

  document.getElementById("totalProducts").textContent = formatCurrency(totalProducts);
  document.getElementById("totalFees").textContent = formatCurrency(totalFees);
  document.getElementById("remainingNet").textContent = formatCurrency(remainingNet);
}

function setImageOnRow(row, file) {
  if (!file || !file.type.startsWith("image/")) {
    return;
  }

  const reader = new FileReader();
  reader.onload = () => {
    const preview = row.querySelector(".photo-preview");
    const text = row.querySelector(".upload-text");
    preview.src = reader.result;
    preview.hidden = false;
    text.hidden = true;
    queueAutosave(true);
  };
  reader.readAsDataURL(file);
}

async function pasteImageFromClipboard(row) {
  try {
    const items = await navigator.clipboard.read();
    for (const item of items) {
      const imageType = item.types.find((type) => type.startsWith("image/"));
      if (imageType) {
        const blob = await item.getType(imageType);
        setImageOnRow(row, new File([blob], "clipboard-image", { type: imageType }));
        return;
      }
    }
  } catch (error) {
    console.error(error);
  }
}

function bindRowEvents(row) {
  row.querySelectorAll(".number-input").forEach((input) => {
    input.addEventListener("input", () => {
      updateSummary();
      queueAutosave(true);
    });
  });

  row.querySelectorAll('input[type="text"], textarea').forEach((input) => {
    input.addEventListener("input", () => queueAutosave(true));
  });

  row.querySelector(".delete-btn").addEventListener("click", () => {
    row.remove();
    updateSummary();
    queueAutosave(true);
  });

  row.querySelector(".photo-input").addEventListener("change", (event) => {
    const [file] = event.target.files;
    setImageOnRow(row, file);
  });

  row.querySelector(".camera-input").addEventListener("change", (event) => {
    const [file] = event.target.files;
    setImageOnRow(row, file);
  });

  row.querySelector(".paste-btn").addEventListener("click", () => {
    pasteImageFromClipboard(row);
  });

  row.addEventListener("paste", (event) => {
    const file = [...(event.clipboardData?.files || [])].find((item) => item.type.startsWith("image/"));
    if (file) {
      event.preventDefault();
      setImageOnRow(row, file);
    }
  });
}

function applyTranslations() {
  const dict = translations[state.language];
  document.documentElement.lang = state.language;
  document.documentElement.dir = state.language === "ar" ? "rtl" : "ltr";
  document.body.dir = state.language === "ar" ? "rtl" : "ltr";
  state.formatter = new Intl.NumberFormat(state.language === "ar" ? "ar-TN" : state.language === "en" ? "en-US" : "fr-FR", {
    minimumFractionDigits: 2,
    maximumFractionDigits: 2
  });

  document.querySelectorAll("[data-i18n]").forEach((element) => {
    const key = element.dataset.i18n;
    if (dict[key]) {
      element.textContent = dict[key];
    }
  });

  document.querySelectorAll(".product-input").forEach((input) => {
    input.placeholder = dict.productPlaceholder;
  });
  document.querySelectorAll('[data-field="reference"]').forEach((input) => {
    input.placeholder = dict.referencePlaceholder;
  });
  document.querySelectorAll(".phone-number-title-input").forEach((input) => {
    input.placeholder = dict.captionDetailPlaceholder;
  });
  document.querySelectorAll(".phone-name-input").forEach((input) => {
    input.placeholder = dict.clientNamePlaceholder;
  });
  document.querySelectorAll(".phone-address-input").forEach((input) => {
    input.placeholder = dict.clientAddressPlaceholder;
  });
  document.querySelectorAll(".notes-input").forEach((input) => {
    input.placeholder = dict.notesPlaceholder;
  });

  document.querySelectorAll("#tableBody tr").forEach((row) => {
    const cells = row.children;
    if (cells[0]) cells[0].dataset.label = dict.product;
    if (cells[1]) cells[1].dataset.label = dict.referenceCode;
    if (cells[2]) cells[2].dataset.label = dict.photo;
    if (cells[3]) cells[3].dataset.label = dict.caption;
    if (cells[4]) cells[4].dataset.label = dict.pickupFee;
    if (cells[5]) cells[5].dataset.label = dict.deliveryFee;
    if (cells[6]) cells[6].dataset.label = dict.productPrice;
    if (cells[7]) cells[7].dataset.label = dict.netTotal;
    if (cells[8]) cells[8].dataset.label = dict.notes;
    if (cells[9]) cells[9].dataset.label = dict.actions;
  });

  updateSummary();
  setExportStatus(localStorage.getItem(STORAGE_KEY) ? "autosaveReady" : "exportReady");
  updateAutosaveMeta();
}

function addRow(shouldAutosave = true) {
  const row = rowTemplate.content.firstElementChild.cloneNode(true);
  tableBody.appendChild(row);
  bindRowEvents(row);
  applyTranslations();
  updateSummary();
  if (shouldAutosave) {
    queueAutosave(true);
  }
}

addRowBtn.addEventListener("click", addRow);
floatingAddBtn.addEventListener("click", addRow);
languageSelect.addEventListener("change", (event) => {
  state.language = event.target.value;
  applyTranslations();
  queueAutosave(true);
});
exportButtons.forEach((button) => {
  button.addEventListener("click", () => {
    handleExport(button.dataset.export);
  });
});
clearSavedDataBtn.addEventListener("click", () => {
  localStorage.removeItem(STORAGE_KEY);
  lastSavedAt = null;
  updateAutosaveMeta();
  setExportStatus("clearSavedDone");
});

if (!restoreFromStorage()) {
  addRow(false);
  addRow(false);
  setExportStatus("autosaveReady");
  updateAutosaveMeta();
}
